<Page x:Class="WPF_Student_Portal.Views.Home.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WPF_Student_Portal.Views.Home"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Background="#F5F5F5"
      Title="HomePage">

    <Grid>
        <Border Padding="20" Height="Auto" Width="Auto">
            <StackPanel Orientation="Vertical">
                <Border Height="Auto" Width="Auto">
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="Admin dashboard" FontFamily="Inter" FontWeight="Bold" FontSize="20" Height="Auto" Foreground="#000000"></TextBlock>
                        <TextBlock Text="Welcome back" FontFamily="Inter" FontWeight="Light" FontSize="10" Height="Auto" Foreground="#605F5F"></TextBlock>
                    </StackPanel>
                </Border>

                <Border HorizontalAlignment="Stretch" Height="Auto" Margin="0, 15, 0, 15">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                        <Border BorderBrush="#9A9A9A" BorderThickness="0.5">
                            <Button Content="Add new" Background="#1FA8F2" Foreground="#FFFFFF" Padding="15, 4, 15, 4" Height="Auto" Width="Auto" FontFamily="Inter" FontStyle="Normal" FontSize="12" FontWeight="Normal"></Button>
                        </Border>

                        <Border BorderBrush="#9A9A9A" BorderThickness="0.5" Margin="10, 0, 10, 0">
                            <Button Content="Edit" Background="#D4EFFD" Foreground="#000000" Padding="15, 4, 15, 4" Height="Auto" Width="Auto" FontFamily="Inter" FontStyle="Normal" FontSize="12" FontWeight="Normal"></Button>
                        </Border>

                        <Border BorderBrush="#9A9A9A" BorderThickness="0.5">
                            <Button Content="Delete" Background="#FF3A44" Foreground="#FFFFFF" Padding="15, 4, 15, 4" Height="Auto" Width="Auto" FontFamily="Inter" FontStyle="Normal" FontSize="12" FontWeight="Normal"></Button>
                        </Border>
                    </StackPanel>
                </Border>
                <!--C:\Users\HP\source\repos\WPF_Student_Portal\WPF_Student_Portal.slnx-->

                <Border CornerRadius="5" BorderThickness="1" BorderBrush="#000000" HorizontalAlignment="Left" Background="#FFFFFF" Width="732" Height="Auto">
                    <DataGrid AutoGenerateColumns="False" 
                              HeadersVisibility="Column"
                              GridLinesVisibility="None"
                              Background="Transparent"
                              BorderThickness="0"
                              FontSize="12"
                              CanUserAddRows="False"
                              HorizontalAlignment="Stretch"
                              ColumnWidth="*">
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Padding" Value="10,5,10,5"/>
                                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>

                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Padding" Value="10,5"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGrid.CellStyle>

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Student ID" Binding="{Binding StudentID}" Width="*"/>
                            <DataGridTextColumn Header="Full name" Binding="{Binding FullName}" Width="*"/>
                            <DataGridTextColumn Header="Contact Email" Binding="{Binding Email}" Width="*"/>
                            <DataGridTextColumn Header="Major" Binding="{Binding Major}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </StackPanel>
        </Border>
    </Grid>
</Page>
